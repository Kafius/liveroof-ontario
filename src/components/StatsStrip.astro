---
const stats = [
  { num: '19', unit: '', label: 'Years in Business' },
  { num: '900', unit: '+', label: 'Projects' },
  { num: '300,000', unit: ' m²', label: 'Installed' },
];
---

<div class="bg-dark py-8 md:py-12">
  <div class="container">
    <div class="stats-grid">
      {stats.map((stat) => (
        <div class="stat-item">
          <div class="stat-num">
            {stat.num}
            {stat.unit && <span class="stat-unit">{stat.unit}</span>}
          </div>
          <div class="stat-label">{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
</div>
